services:
  server:
    build:
      context: .
      dockerfile: apps/server/Dockerfile
    ports:
      - 8000:8000

  web:
    build:
      context: .
      dockerfile: apps/web/Dockerfile
    ports:
      - 3000:3000
    depends_on:
      - server
